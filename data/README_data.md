# Project IRIS — Data

## Files
- frb_runs.csv: Run ledger (one row per experiment run)
- frb_catalog.csv: FRB event catalog (minimal schema; expand as needed)

## Conventions
- Keep schemas stable; add new columns rather than renaming.
- Use run_id as the primary key for linking plots and notes.
